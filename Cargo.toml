[package]
name = "exceptional"
version = "0.1.0"
authors = ["Guillaume Malette <guillaume@shopify.com>"]
build = "build.rs"

[dependencies]
num = "0.1"
regex = "0.2"

[build-dependencies]
peg = { version = "0.4" }
